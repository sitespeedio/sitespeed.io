<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>sitespeed.io-release-feed</id>
    <title>sitespeed.io release feed</title>
    <updated>2026-05-31T00:00:00.000Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <author>
        <name>Peter Hedenskog</name>
        <email>peter@soulgalore.com</email>
        <uri>https://www.peterhedenskog.com</uri>
    </author>
    <link rel="alternate" href="https://www.sitespeed.io"/>
    <link rel="self" href="https://www.sitespeed.io/feed/atom.xml"/>
    <subtitle>New releases and changelog feed of sitespeed.io</subtitle>
    <logo>https://www.sitespeed.io/img/logos/sitespeed.io.png</logo>
    <icon>http://www.sitespeed.io/favicon.ico</icon>
    <rights>All rights reserved 2022, Peter Hedenskog and team</rights>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 41.2.1]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.2.1</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.2.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-31T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Unblock the arm64 Docker release build by bumping the QEMU <code>binfmt</code> emulator to a 9.2 build that handles modern systemd, and pinning APT so only the Firefox package is pulled from Debian unstable #4771.<br>
- Move the slim Docker image to Debian trixie (the new stable) and drop the APT pin from #4771, which was too strict to let Firefox&#39;s <code>libc6</code> / <code>libnss3</code> dependencies upgrade from unstable #4772.<br>
- CI: allow Docker Hub&#39;s CloudFront CDN (<code>production.cloudfront.docker.com</code>) through the Linux workflow&#39;s Harden-Runner egress allowlist, so the test job stops flaking when DNS routes the runner to CloudFront instead of Cloudflare #4773.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 41.2.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.2.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-26T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- JS/CSS coverage in the report. Browsertime 27.4.0 collects per-iteration coverage when you pass <code>--chrome.coverage</code>, and the HTML report now surfaces it as a Coverage section in the per-URL view: median JS/CSS total/unused/percent KPIs, first-party vs third-party split, top-25 worst-offender tables, and a methodology + recommendations panel. <code>--chrome.coverage</code> is declared in sitespeed.io&#39;s own CLI so it shows up in <code>--help</code> and the config reference, and the <code>--enableProfileRun</code> path now collects coverage on the profile run and merges it into the main result #4769, #4767.<br>
<br>
<strong>Fixed</strong><br>
- Visual-change lines (<code>_firstVisualChange</code> / <code>_lastVisualChange</code> / <code>_visualComplete85</code>) are back on the waterfall when the HAR is gzipped on disk (the default). The renderer&#39;s user-timing injection was parsing the raw gzip bytes as JSON and silently falling back to the unannotated HAR; it now sniffs the gzip magic bytes and inflates via <code>DecompressionStream</code> first #4758.<br>
- Per-request detail panel on the waterfall now shows real data again. The panel was reading HAR-shape fields (<code>req.response.content.size</code>, <code>req.response.headers</code>, <code>req.response.status</code>, <code>req.timings</code>, …) but waterfall-tools&#39; click payload is its own flat internal <code>Request</code> object, so Response body always showed <code>0 B</code>, Status was blank, MIME type and Protocol were <code>—</code>, and the response-headers and timing tabs were empty. Look up the matching HAR entry by index and read the rich HAR shape from it, falling back to the waterfall-tools shape only when the HAR is missing.<br>
- Folder path aliases now accept Unicode letters and digits, so non-ASCII URLs no longer collapse to <code>-</code> in result paths (issue #3880) #4759.<br>
- <code>scp</code> plugin: retry the initial SFTP handshake up to 3× with linear backoff. <code>ssh2-sftp-client</code> v12 removed its built-in connect retries, so a single &quot;Connection lost before handshake&quot; blip was failing the whole upload #4760.<br>
- Graphite: retry the post-run annotation POST up to 3× on network errors and 5xx responses. 4xx still fails fast #4761.ß<br>
- <code>waterfall-tools</code> bumped to the first proper npm release, 0.3.0 — no more pinning to a GitHub tarball, and the bundling script drops to ~30 fewer lines now that the published browser bundle has the platform aliases baked in #4764.<br>
- Browsertime 27.4.0 #4767.<br>
- Docker base image rev&#39;d to <code>chrome-148.0-firefox-150.0-edge-147.0-d</code> #4762.<br>
- Slim Docker image cleanup #4757.<br>
- Drop now-unused npm <code>overrides</code> from <code>package.json</code> #4756.<br>
- CI: PR Docker workflow now builds and exercises the image on arm64 in addition to amd64, so arch-specific breakage surfaces pre-merge instead of at release-tag time. Edge step is skipped on arm64 (Linux Edge is amd64-only) #4766.<br>
- CI: pin the Safari workflow to <code>macos-14</code> #4765.<br>
- Drop an unnecessary <code>\.</code> escape inside a character class in <code>pathToFolder</code> that was tripping eslint&#39;s <code>no-useless-escape</code> and failing lint on every PR #4763.<br>
- Docs: remove unused CSS #4768.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.4.1]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.4.1</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.4.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-25T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- <code>--chrome.coverage</code> no longer reports &quot;0 B unused&quot; for functions that executed at least once. The JS used-bytes calculation took the union of every V8 range with count &gt; 0, but V8 returns nested ranges — an outer range over the whole function body and inner ranges over sub-blocks. If a function ran at all, its outer range already covered the whole body and the inner count == 0 ranges (the actual dead branches) got absorbed into the union and contributed nothing. On modern bundles where module-evaluation top-level code runs for almost every function, the per-script unused-% collapsed to roughly zero across the board, leaving sitespeed.io rendering a &quot;0 B unused&quot; column on pages that obviously ship plenty of dead code. The calculation now walks the ranges as nested intervals, painting the innermost range&#39;s count at every byte (the same definition Chrome DevTools&#39; Coverage panel uses), so dead branches inside executed functions show up correctly. CSS rule-usage tracking returns flat, non-overlapping ranges and is unchanged #2484.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.4.0]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.4.0</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.4.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-24T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- JS/CSS coverage collection for Chrome. <code>--chrome.coverage</code> turns it on for every iteration for users who want the data and accept that detailed V8 coverage deoptimizes scripts and will skew their timing metrics. <code>--enableProfileRun</code> also turns coverage on for Chrome alongside the existing trace, so users who want coverage without affecting their timings can lean on the same extra-iteration pattern they already use for tracing — the result is merged back into the main <code>browsertime.json</code> so consumers like sitespeed.io see the data in the same place they read every other metric. Each entry has a per-file breakdown (<code>url</code>, <code>totalBytes</code>, <code>usedBytes</code>, <code>unusedBytes</code>, <code>unusedPercent</code>) #2480.<br>
<br>
<strong>Fixed</strong><br>
- HAR post-processing no longer throws when a page has no matching entries. Occasionally a HAR contains a page whose <code>pageref</code> has no entries — typically a navigation that didn&#39;t produce any requests, or an extension-injected page. <code>getDocumentRequests</code> and <code>getFinalURL</code> dereferenced <code>shift()</code> / <code>pop()</code> without a guard, and <code>getMainDocumentTimings</code> wrapped its entire per-page loop in a single try/catch so one bad page wiped out the main-document timings for every other page in the same HAR. Downstream this surfaced as a noisy triplet of warnings followed by an empty coach-data payload in sitespeed.io. The three crash points are now guarded and unresolvable pages are skipped #2483.<br>
- Bumped <code>geckodriver</code> to a version with native ARM-64 support #2482.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 4.0.1]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#4.0.1</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#4.0.1"/>
        <updated>2026-05-22T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Compare page now renders screenshots, the filmstrip and the run video when the HAR points at a different sitespeed.io result server (e.g. wikiperformance comparing HARs that live on wikimedia.sitespeed.io). The helmet CSP only allowed <code>&#39;self&#39;</code> for images and — via the inherited <code>default-src</code> — for video, so the cross-origin URLs the HAR carries in <code>_meta.screenshot</code> / <code>_meta.filmstrip[].file</code> / <code>_meta.video</code> were blocked and the slots came up empty. <code>img-src</code> now also allows any <code>https:</code> origin and a new <code>media-src</code> does the same for video #269.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 41.1.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.1.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.1.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-20T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- New <code>rsync</code> plugin for result uploads. The existing <code>scp</code> plugin walks files one at a time over a single SFTP channel, which is the worst case for a result directory of thousands of small files. The new plugin shells out to <code>rsync</code> over <code>ssh</code>, pipelines the wire protocol and reuses the channel — the same upload finishes in a small fraction of the time on a typical Linux host. Exposed as <code>--rsync.host</code> / <code>--rsync.username</code> / <code>--rsync.privateKey</code> / <code>--rsync.destinationPath</code> etc., sitting alongside <code>scp</code>, <code>s3</code> and <code>gcs</code>; existing scp users keep their exact behaviour and opt in to rsync only when they want it #4739.<br>
- Upload performance on the existing <code>scp</code> plugin: swapped <code>node-scp</code> (serial uploads, stuck on 0.0.25) for <code>ssh2-sftp-client</code>, which fans out 16 concurrent uploads on the same ssh2 transport. Same auth, same options, same CLI surface — just faster on result directories with many small files #4738.<br>
<br>
<strong>Fixed</strong><br>
- Bring back the visual-change markers on the waterfall. The renderer that replaced PerfCascade in #4614 only drew vertical lines for a fixed set of page-event names, so the <code>_firstVisualChange</code> / <code>_lastVisualChange</code> / <code>_visualComplete85</code> lines silently disappeared with the swap. They&#39;re now hoisted into the user-timing-marks bag the renderer does draw, the &quot;Marks &amp; visual metrics&quot; toggle is on by default, and the hover tooltip identifies which metric or user mark the cursor is on #4755.<br>
- Browsertime 27.3.0 #4754.<br>
- <code>coach-core</code> bumped to 9.2.1 #4750.<br>
- Bump <code>@sitespeed.io/log</code> and the bundled plugin to latest #4746.<br>
- Crawler rewrite: <code>simplecrawler</code> (unmaintained since 2020, ~1000 lines of transitive code) is replaced by an in-tree <code>node:fetch</code>-based crawler in ~285 lines with no new dependency. Same BFS semantics, same depth/maxPages/include/exclude/content-type/skip-extension rules, same robots.txt handling (<code>--crawler.ignoreRobotsTxt</code> still opts out), same cookies/basicAuth/userAgent passthrough. Parity verified across four real-site runs at different depths and page caps; natural completion is ~6× faster on the sample runs #4745.<br>
- Dependency pruning, no behaviour change for users: <code>lodash.get</code>, <code>lodash.set</code> and <code>lodash.merge</code> are replaced by small in-tree helpers in <code>lib/support/objectPath.js</code>, with the <code>merge</code> replacement verified byte-for-byte against <code>lodash.merge</code> across 23 edge cases and an A/B <code>parseCommandLine()</code> run. <code>dayjs</code> (plus the utc plugin) is replaced by an ~80-line <code>lib/support/time.js</code>, with every <code>.format()</code> pattern actually used in the codebase pinned in a new test. <code>ora</code> is replaced by a ~60-line blinking-dot spinner used only by <code>--api.add</code> / <code>--api.addAndGetResult</code>. The lodash subtree is now gone entirely [#4737, #4741, #4742, #4744, #4740].<br>
- The <code>markdown</code> template helper exposed to pug templates is removed. It was added in 2018 (#2161) but no shipped <code>.pug</code> template has ever called it, and the underlying <code>markdown@0.5.0</code> package has been unmaintained for over a decade with an open ReDoS advisory. Anyone with a custom pug template that calls <code>markdown(...)</code> will need to import their own renderer #4743.<br>
- Slimmer main Docker image: <code>apt-get install --no-install-recommends</code> + <code>/var/lib/apt/lists</code> cleanup, <code>npm ci</code> (lockfile-strict) instead of <code>npm install</code>, and the <code>selenium-webdriver/bin</code> cleanup folded into the install layer so the deleted bytes no longer ship in an earlier layer #4753.<br>
- Docker: use <code>--omit=dev</code> instead of the deprecated <code>--production</code>, and fix the <code>skip edgedriver</code> typo #4752.<br>
- Release tooling: bump <code>feed</code> to 5.2.1 and <code>marked</code> to 18.0.3 (used only by <code>release/feed.js</code> when refreshing documentation RSS feeds). API usage unchanged, regenerated feeds are content-identical #4751.<br>
- Dev tooling: align with eslint 10 and unicorn 64, drop the dead legacy <code>.eslintrc.json</code> and the stale <code>eslint-check</code> script, minor bumps to prettier/sass/esbuild #4747.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.3.0]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.3.0</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.3.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-20T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- <code>browsertime --help</code> is now navigable by topic. The default view shows a short curated list of common options plus the available topic names. <code>browsertime --help &lt;topic&gt;</code> shows just that topic, and <code>browsertime --help-all</code> reproduces the historical full dump for scripts and power users. The old single-screen dump of every option from all 13 topic groups was overwhelming for new users and slow to skim; the topic-filtered view follows the curl/ffmpeg pattern #2467.<br>
<br>
<strong>Fixed</strong><br>
- TypeScript types and IntelliSense for navigation scripts are tightened across the script-facing surface. A new <code>BrowsertimeScript</code> type alias lets scripts annotate their default export and get both <code>context</code> and <code>commands</code> typed in one line. The high-level command helpers (<code>navigate</code>, <code>find</code>, <code>getText</code>, <code>fill</code>, <code>hover</code>, <code>press</code>, <code>getTitle</code>, <code>waitForUrl</code>, …) now surface their JSDoc parameter and return types in IntelliSense instead of being typed as <code>Function</code>, and <code>commands.find</code> resolves to a real <code>WebElement</code> so chained calls get completion from selenium-webdriver #2476. <code>context.options</code>, <code>context.result</code>, and <code>context.taskData</code> are typed as <code>Record&lt;string, any&gt;</code> so editors offer completion on <code>context.options.</code> etc. #2478. <code>commands.mouse</code> is now a proper shape (selector typed as <code>string</code>, <code>singleClick</code>&#39;s options surface <code>waitForNavigation</code>) instead of an anonymous object literal, and <code>measure.stop</code> / <code>measure.clickAndMeasure</code> / <code>measure.stopAsError</code> return types are explicit (<code>Promise&lt;void&gt;</code> / <code>Promise&lt;unknown&gt;</code>) instead of <code>Promise&lt;any&gt;</code> #2479.<br>
- How Browsertime publishes its TypeScript types is now safer. <code>scripting.d.ts</code> moved to the repo root next to <code>package.json</code> so it can&#39;t be clobbered by a future <code>tsc</code> outDir change, the <code>types</code> condition comes first under <code>exports</code> to match TypeScript&#39;s documented resolution rules, and <code>publint</code> runs as part of <code>prepublishOnly</code> so common package.json mistakes fail before a release goes out #2477.<br>
- Replaced the direct <code>lodash.merge</code> dependency with a small in-tree <code>merge()</code> helper in <code>lib/support/util.js</code>. The helper matches lodash&#39;s actual semantics for the cases Browsertime relies on — arrays merged by index, <code>undefined</code> source values skipped unless the key is missing on the target, <code>Date</code>/<code>RegExp</code>/class instances passed by reference, prototype-pollution keys rejected — which finishes the lodash unbundling the same way sitespeed.io did #2468.<br>
- Bumped dependencies: <code>chrome-har</code> 1.3.1 #2469, <code>chrome-remote-interface</code> and <code>selenium-webdriver</code> #2475, <code>@sitespeed.io/log</code> 2.0 #2471, <code>chromedriver</code> / <code>edgedriver</code> / <code>geckodriver</code> #2472.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.3.1]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.3.1</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.3.1"/>
        <updated>2026-05-19T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 9.2.1]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.2.1</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.2.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2026-05-18T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- 9.2.0 generated JS was broken, fixed in #177.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 9.2.0]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.2.0</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2026-05-17T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Vendor the Wappalyzer engine in <code>lib/technologies/wappalyzer.js</code> and drop the deprecated <code>wappalyzer-core</code> npm package. The upstream package is unmaintained on npm, ships with a <code>Proprietary</code> license tag, and prints a deprecation warning on every install even though it is a few hundred lines of zero-dependency pattern matching. The port keeps the same public surface (<code>setTechnologies</code>, <code>setCategories</code>, <code>analyze</code>, <code>resolve</code>), the same pattern parsing, and the same implies/excludes resolution — behaviour is unchanged on the existing technology tests (real WordPress HAR fixture, plus the CSP-leak and third-party-headers safety checks). <code>getWappalyzerCoreVersion()</code> now reports coach-core&#39;s own version, which is the thing that actually changes when the engine changes; the data version stays separately available via <code>getTechnologiesVersion()</code> #169.<br>
<br>
<strong>Fixed</strong><br>
- Shrink the runtime dependency footprint from eight packages to two. <code>lodash.sortby</code>, <code>json-stable-stringify</code> and the dev-only <code>bookmarkletify</code> were declared but referenced nowhere; <code>lodash.merge</code>, <code>lodash.groupby</code>, <code>filter-files</code> and the dev-only <code>mkdirp</code> were one-call helpers replaced with small inline equivalents (the new <code>lib/support/objectMerge.js</code> is ported from the same helper sitespeed.io uses in place of <code>lodash.merge</code>, where it was verified against the original across 23 edge cases). <code>dependencies</code> is now just <code>pagexray</code>, <code>third-party-web</code> and the vendored Wappalyzer engine #165, #166, #167, #168.<br>
- Drop <code>connect</code> and <code>serve-static</code> from the test fixture web server in favour of a small native <code>http</code> / <code>http2</code> handler. The fixture only needed to map a request path to a file under <code>test/http-server</code> and stream it back, optionally over http2 with the test certs — a Connect middleware pipeline plus <code>serve-static</code> was overkill, and was the last reason to depend on a Connect middleware stack at all #170.<br>
- Migrate the test suite from <code>mocha</code> + <code>chai</code> + <code>chai-as-promised</code> to <code>ava</code>, the framework sitespeed.io and browsertime already use. All 45 test files are converted: nested <code>describe</code> / <code>it</code> is flattened into ava&#39;s flat <code>test(&#39;Suite / Case&#39;, t =&gt; ...)</code> form so the old grouping stays visible in test names; chai&#39;s <code>should</code> / <code>expect</code> assertions are mapped onto the corresponding <code>t.is</code>, <code>t.deepEqual</code>, <code>t.truthy</code>, <code>t.regex</code>, <code>t.true</code> / <code>t.false</code>. Browser-driven tests keep their per-browser runner setup, but as a single module-level <code>Map</code> populated in <code>test.before</code> and torn down in <code>test.after.always</code>, with <code>test.serial</code> + <code>t.timeout(60_000)</code> per case #171.<br>
- Bump <code>third-party-web</code> to 0.29.2 #174.<br>
- Update to ESLint 10 and align with the sitespeed.io ESLint stack: <code>eslint</code> 9.17 → 10.4, <code>eslint-config-prettier</code> 9.1 → 10.1.8, <code>eslint-plugin-prettier</code> 5.2 → 5.5.5. <code>@eslint/js</code> and <code>globals</code> were silently transitive under v9 and are now declared explicitly. Drop <code>FlatCompat</code> in favour of the flat-config shape ESLint 10 expects natively. Six new <code>eslint:recommended</code> findings (<code>no-useless-assignment</code> and <code>preserve-caught-error</code>) are fixed in place: five <code>let foo = &#39;&#39;</code> initializers where every code path reassigned before reading become <code>let foo;</code>, and a rethrow in the wappalyzer engine grows a <code>{ cause: error }</code> so the original error chains through #175.<br>
- Adopt <code>eslint-plugin-unicorn</code> 64, the same major sitespeed.io and browsertime are on. The eslint config moves from <code>.cjs</code> to <code>.js</code> (ESM) on the way — <code>eslint-plugin-unicorn</code> v64 is ESM-only and the package was already <code>type: module</code>. The unicorn rule set surfaced 354 findings, 276 of them auto-fixed: a sweep toward modern array methods (<code>for…of</code> over <code>.forEach</code>, <code>flatMap</code> over <code>map().flat()</code>, <code>some</code> over <code>findIndex</code> truthy checks), node-native helpers (<code>Number.parseInt</code>, <code>String.fromCodePoint</code>, <code>structuredClone</code>, <code>String#slice</code>, <code>String#replaceAll</code>, <code>String.raw</code>, <code>includes</code>), and tidier conditionals (explicit length checks, no nested ternaries, no useless <code>undefined</code>). The remaining hand-fixes are small. The config disables the same six unicorn rules sitespeed.io disables, plus three more that don&#39;t fit coach-core&#39;s situation: <code>no-null</code> (the vendored wappalyzer engine deliberately uses <code>null</code> to match the upstream data format), <code>import-style</code> (destructured <code>node:*</code> imports are clearer than forcing default imports), and <code>prefer-module</code> scoped to <code>lib/dom/**</code> (those files are not modules — they are IIFE fragments that get concatenated into <code>dist/coach.min.js</code> and evaluated in the browser via WebDriver <code>executeScript</code>) #176.<br>
- The <code>terser</code> build script was running with <code>--compresss</code> (three s&#39;s). Terser silently treated the typo as an unknown flag and skipped compression entirely, so the shipped <code>coach.min.js</code> was mangled-only. Correcting it to <code>--compress</code> shrinks the bundle from 54,739 to 52,739 bytes — a ~2 KB win on every release with zero behaviour change #173.<br>
- Drop the unrunnable <code>eslint-check</code> npm script. It piped <code>eslint --print-config .eslintrc.js</code> (the file is now <code>eslint.config.cjs</code>) into <code>eslint-config-prettier-check</code>, a binary modern <code>eslint-config-prettier</code> no longer ships — the script could not run, and removing it removes confusion #173.<br>
- Backfill HAR tests for fourteen previously untested rules: the eleven HAR privacy rules without coverage (<code>crossOriginEmbedderPolicyHeader</code>, <code>crossOriginOpenerPolicyHeader</code>, <code>crossOriginResourcePolicyHeader</code>, <code>googleReCaptcha</code>, <code>mixedContent</code>, <code>nelHeader</code>, <code>permissionsPolicyHeader</code>, <code>reportingEndpointsHeader</code>, <code>thirdPartyCookies</code>, <code>thirdPartyPrivacy</code>, <code>xContentTypeOptionsHeader</code>), the HAR-level <code>avoidRenderBlocking</code>, and the third-party rules in <code>bestpractice/thirdParty</code> and <code>info/thirdParty</code>. Header-based rules use a clone-and-inject-header pattern; the third-party rules lean on the existing <code>www.nytimes.com.har</code> fixture with <code>firstParty: &#39;nytimes\\.com&#39;</code> set so pagexray classifies off-domain assets correctly. Test count rises from 67 to 101 #172.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 41.0.1]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.0.1</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.0.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-16T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Drop unused fast-crc32c dependency #4735<br>
- Fix visual-progress curve and scrubber to match the data&#39;s semantics<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 41.0.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.0.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#41.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-15T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- HTTP/3 / HTTP/3.0 connections are now classified as <code>h3</code> instead of falling through to <code>h1</code>, so the HTTP-version breakdown is correct on HTTP/3 traffic.<br>
- <code>Cache-Control</code> is parsed case-insensitively (RFC 7234), so <code>Max-Age=42</code> is read as 42 instead of 0 and <code>No-Cache</code> / <code>No-Store</code> are honoured — caching advice and <code>expireStats</code> no longer over-report problems on origins that capitalise the directive.<br>
- <code>missingCompression</code> no longer flags already-gzipped assets (the encoding check was comparing a flattened header array against a string), accepts compound encodings like <code>br, gzip</code>, and recognises <code>zstd</code>. <code>xml</code> is now also considered for compression reporting.<br>
- The <code>Domain=</code> cookie attribute is matched case-insensitively (RFC 6265), so third-party cookies set with lowercase <code>domain=</code> are detected.<br>
- <code>getMainDomain</code> recognises common two-label public suffixes (<code>.co.jp</code>, <code>.com.br</code>, <code>.com.au</code>, …), so the auto-generated first-party regex stops misclassifying same-site requests on non-<code>.co.uk</code> country domains.<br>
- <code>defaultContentTypes</code> now includes <code>favicon</code>, so every page has a consistent shape in the content-type breakdown.<br>
- <code>getDocumentRequests</code> returns an empty array for pages with no matching entries instead of throwing.<br>
<br>
<strong>Changed</strong><br>
- <code>coach-core</code> bumped to 9.1.0, which pulls in <code>pagexray</code> 5.0.0 #4731.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 4.0.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#4.0.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#4.0.0"/>
        <updated>2026-05-15T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- New <code>failed_reason TEXT</code> and <code>finished_date TIMESTAMP</code> columns on <code>sitespeed_io_test_runs</code>. <code>failed_reason</code> is populated from three paths the DB previously knew nothing about: Bull&#39;s <code>global:failed</code> event (the testrunner threw), the queue-down submit path in <code>add-test.js</code> (Redis unreachable when the test was queued), and the result-queue path when sitespeed.io itself exits non-zero (testrunner now sends <code>Test runner exited with code N: &lt;last line of stderr&gt;</code>, capped at 500 chars). <code>finished_date</code> is stamped on every terminal transition, so end-to-end duration can be derived as <code>finished_date - added_date</code> and run duration as <code>finished_date - run_date</code> (which is the browsertime <em>start</em> timestamp, not finish) #264.<br>
- <code>server/database/migrations/</code> directory establishes the convention for schema changes against existing deployments — <code>setup.sql</code> only runs on a fresh Postgres data dir, so live databases need ALTERs applied separately. Each migration ships with a <code>.sh</code> helper (<code>check</code> / <code>backup</code> / <code>dry-run</code> / <code>apply</code> / <code>verify</code> / <code>rollback</code>) that runs <code>psql</code> inside the compose Postgres container, plus a README documenting the manual path. Existing deployments must run <code>001-add-failure-fields.sh apply</code> before rolling the server forward #264.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.8.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.8.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.8.0"/>
        <updated>2026-05-15T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Default sitespeed.io Docker image bumped from 40 to 41 in <code>.env.example</code>. The README &quot;Update sitespeed.io version&quot; snippets now reference 41 / 42 as the current and next-major tags.<br>
- Vendored compare bundle rolled to pagexray 5.0.0 (HTTP/3 detection, case-insensitive <code>Cache-Control</code> directives, <code>missingCompression</code> over-count fix). The bundle shrinks from ~28 KB to ~17 KB thanks to pagexray&#39;s switch from uglify-es to terser #262.<br>
<br>
<strong>Fixed</strong><br>
- <code>/admin</code> &quot;failed&quot; pill is now a real health signal: it counts failed test runs in the last 24 h from Postgres instead of Bull&#39;s <code>getJobCounts()</code>, which was capped by <code>removeOnFail</code> and only decayed on a successful retry — so the pill accumulated weeks of stale failures and shouted &quot;13 failed&quot; next to activity cards that correctly showed 0 in 24 h #261.<br>
- Compare cookie list no longer renders domains like <code>.example.com\nUIDR=...</code> when a HAR concatenates multiple <code>Set-Cookie</code> response headers into one newline-joined value — the next cookie&#39;s name was leaking into the previous cookie&#39;s <code>Domain=</code> attribute. Picked up via the pagexray bump #262.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 40.5.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.5.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.5.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-14T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Scripts are now auto-detected on the command line, so <code>--multi</code> is no longer required to run them. Any positional argument that looks like a script — <code>.js</code> / <code>.cjs</code> / <code>.mjs</code>, or any file whose first non-empty line doesn&#39;t start with <code>http</code> — flips multi mode on automatically, and mixed inputs like <code>login.js https://example.com logout.js</code> just work. <code>--multi</code> is kept as an explicit override for the one case auto-detection can&#39;t cover: sharing a single browser session across a list of plain URLs. All existing invocations behave exactly as before #4725.<br>
<br>
<strong>Fixed</strong><br>
- Gzipped HAR files are now written by piping JSON through <code>createGzip</code> straight to disk instead of materialising the JSON string, a <code>Buffer</code> copy of it, and the full gzipped <code>Buffer</code> all at once. For a 200 MB HAR that removes several hundred MB of avoidable peak RSS, multiplied when multiple pages finish around the same time. The storage layer now accepts a <code>Readable</code> in addition to strings and Buffers; existing callers are unaffected #4728.<br>
- Gzipped JSON result files (Chrome traces, console logs, etc.) are read by streaming through <code>createGunzip</code> and collecting utf-8 chunks rather than buffering the whole gzipped payload, gunzipping it into another Buffer, then stringifying. The parsed object still has to fit in memory, but the throwaway gzipped and unzipped buffer copies are gone — meaningful on 50+ MB traces #4726.<br>
- The sustainable plugin and the S3 plugin pick up the same streaming treatment: the sustainable plugin now uses the shared streaming gzipped-JSON helper instead of its own buffer-everything copy, and the S3 plugin streams uploads via <code>createReadStream</code> with an explicit <code>ContentLength</code> instead of loading each file fully into memory before <code>PutObject</code>. With 20 concurrent S3 uploads, RSS no longer spikes to ~20× the size of the largest file in the result bundle #4727.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 9.1.0]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.1.0</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.1.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2026-05-14T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Updated to PageXraty 5.0.0 #164.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 5.0.0]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#5.0.0</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#5.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2026-05-14T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- CLI can now read a HAR from stdin: <code>pagexray -</code> or piping into <code>pagexray</code> with no path argument #131.<br>
- CLI prints a friendly error to stderr and exits 1 when the file is missing or the input isn&#39;t valid JSON (previously threw a raw Node stack trace) #131.<br>
- README updated to drop the long-removed <code>cookieStats</code> example block and document fields added across 3.x / 4.x (<code>fullyLoaded</code>, <code>afterOnLoad</code>, <code>afterOnContentLoad</code>, <code>renderBlocking</code>, <code>googleWebVitals</code>, <code>cpu</code>, <code>firstPartyRegEx</code>) #130.<br>
<br>
<strong>Fixed</strong><br>
- Classify <code>HTTP/3</code> / <code>HTTP/3.0</code> connections as <code>h3</code> (previously only the lowercase <code>h3</code> / <code>h3-29</code> shorthand was recognised — the canonical form fell through to <code>h1</code>) #128.<br>
- Parse <code>Cache-Control</code> directives case-insensitively (RFC 7234 §5.2). <code>Max-Age=42</code> is now read as 42 instead of 0, and <code>No-Cache</code> / <code>No-Store</code> are honoured #128.<br>
- Replace the deprecated <code>url.parse</code> (Node DEP0169) with the WHATWG <code>URL</code> parser in <code>getHostname</code> #128.<br>
- Stop throwing in <code>getDocumentRequests</code> when a page has no matching entries — return an empty array instead #128.<br>
- <code>missingCompression</code> was flagging every large text asset, including gzipped ones, because <code>headers.flatten</code> wraps values in arrays and the encoding check compared the array to a string. Unwrap before comparing, accept compound codings (<code>br, gzip</code>), and recognise <code>zstd</code> #129.<br>
- Match the <code>Domain=</code> cookie attribute case-insensitively (RFC 6265 §5.2) so third-party cookies set with lowercase <code>domain=</code> are detected #129.<br>
- Recognise common two-label public suffixes (<code>.co.jp</code>, <code>.com.br</code>, <code>.com.au</code>, ...) in <code>getMainDomain</code> so the auto-generated first-party regex isn&#39;t wrong for non-<code>.co.uk</code> country domains #129.<br>
- <code>defaultContentTypes</code> now includes <code>favicon</code> so every page has a consistent shape (matches the example in the README) #129.<br>
- <code>xml</code> is now considered for <code>missingCompression</code> reporting #129.<br>
- <code>engines.node</code> bumped from <code>&gt;=8.9.0</code> to <code>&gt;=22.0.0</code> (the current Node.js LTS), and the GitHub Actions matrix now runs on Node 22 + 24 #132, #133.<br>
- Test runner switched from Mocha + Chai to AVA, matching the rest of the sitespeed.io / browsertime ecosystem. Test helpers no longer depend on bluebird or lodash #133.<br>
- ESLint upgraded from 8 to 9 with the new flat config (<code>eslint.config.js</code> replaces <code>.eslintrc.json</code> and <code>.eslintignore</code>) #133.<br>
- All devDependencies bumped to current major versions: prettier 3, browserify 17, browserify-banner 2, @babel/core + preset-env 7.29, jsdoc 4. The abandoned <code>uglify-es</code> is replaced by <code>terser</code>, and <code>mkdirp</code> is dropped in favour of native <code>fs.mkdirSync({recursive:true})</code> in the browserify script #133.<br>
<br>
<strong>Changed</strong><br>
- Node.js 22 is minumium.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 40.4.1]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.4.1</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.4.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-13T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Stop parsing scripting files as URL-alias lists in <code>getAliases</code> #4724.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.7.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.7.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.7.0"/>
        <updated>2026-05-13T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Per-queue 24 h sparkline on each <code>/admin</code> queue row, independently scaled so low- and high-traffic queues both show shape #256.<br>
- Subtle motion on <code>/admin</code>: one-time staggered bar-chart entrance, refresh-indicator pulse, and tweened number rollups when totals change. All honour <code>prefers-reduced-motion</code> #256.<br>
<br>
<strong>Fixed</strong><br>
- Queue defaults bumped to <code>removeOnComplete: 50 / removeOnFail: 100 / attempts: 2</code>; code-level fallbacks aligned with the yaml #255.<br>
- Internal queues (<code>testrunners</code>, <code>result</code>) no longer listed on <code>/admin</code> — they had no failures, no sparkline, and an inert Empty button #258.<br>
- Refresh indicator on <code>/admin</code> now sits in a fixed-width column so the version pill can&#39;t shift sideways each tick #258.<br>
- Testrunner setup duplicated on restart — registration was concatenating old + new instead of deduping #257.<br>
- Multi-type setups on one location (e.g. desktop + emulatedMobile) collapsed into the last entry after a restart; dedupe now keys on full setup identity, not just queue name #259.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.6.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.6.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.6.0"/>
        <updated>2026-05-13T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Activity stats on <code>/admin</code>: totals for the last 24 hours and 7 days plus a stacked bar chart of tests per hour over the last 24 h (completed / failed / other). Pure server-rendered SVG, cached for 60 s so the auto-refresh doesn&#39;t beat on Postgres #252.<br>
<br>
<strong>Fixed</strong><br>
- Hide the Failed count for the internal <code>testrunners</code> and <code>result</code> queues — the number was unactionable because the queues don&#39;t appear in Recent failures #251.<br>
- Age labels in Recent failures now graduate to <code>d</code>/<code>mo</code> past 48 h instead of shouting <code>12608h ago</code> #251.<br>
- Long URLs no longer push the <code>/search/</code> results table past its container and shove the page off-centre. URL cells now wrap at any character; the form has <code>overflow-x: auto</code> as a safety net #253.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.5.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.5.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.5.0"/>
        <updated>2026-05-13T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Health banner on <code>/admin</code>: Redis up/down, Postgres up/down, connected testrunners, totals for pending/running/failed jobs #247.<br>
- &quot;Currently running&quot; table on <code>/admin</code> — every active job with URL/script, queue, runner, and elapsed time. Runs over 10 minutes are highlighted #247.<br>
- &quot;Recent failures&quot; table on <code>/admin</code> with the error message and a one-click Retry button #248.<br>
- Active and Failed counts on the queue table, plus a red &quot;no worker&quot; badge when a queue has pending jobs but no testrunner serving it #246.<br>
- <code>/admin</code> auto-refreshes every 15 seconds. Pauses in background tabs #249.<br>
- Opt-in auto-refresh toggle on <code>/search/</code>, 60 seconds. Off by default. Pauses while any test is ticked, so Compare selections aren&#39;t lost. State remembered in <code>localStorage</code> #250.<br>
- <code>/admin</code> mobile layout reworked for the new tables — rows stack into name → counts → action on narrow screens #246 #247 #248.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.4.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.4.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.4.0"/>
        <updated>2026-05-13T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- <strong>Connected testrunners</strong> table on <code>/admin</code> showing hostname, location, the setup each machine serves, and a fresh / stale &quot;last seen&quot; badge. Each testrunner publishes a heartbeat on the existing <code>testrunners</code> Bull queue every 30 s; the server tracks <code>lastSeenAt</code> per runner and prunes anything quiet for &gt;120 s, so crashed runners (OOM, host reboot, <code>docker kill</code>) drop off the admin page within ~2 minutes instead of haunting the metrics gauge forever. When two runners pick the same <code>LOCATION_NAME</code> + <code>deviceId</code>, the server logs a clear <code>Testrunner queue collision: hostA and hostB both claim queue X. Change LOCATION_NAME (or deviceId) on one of them.</code> — the registration still goes through so existing deployments don&#39;t break, but the collision is no longer silent #244.<br>
- <code>deploy/PRODUCTION.md</code> gets an &quot;Adding a third (or Nth) testrunner&quot; section with three concrete troubleshooting cases: not registering, stale row, queue collision #244.<br>
- <strong>Compare workflow on <code>/search/</code></strong>: the column over the per-row checkbox is now its own <code>Compare</code> header (split out from the generic &quot;Actions&quot;), a one-line helper below the table explains the affordance, and each checkbox carries an <code>aria-label</code> / <code>title</code> of &quot;Select this run to compare with another&quot;. The Compare button itself starts disabled, gates submission until exactly two completed runs are ticked (a third tick is refused — the unchecked boxes go disabled when the limit is hit), and shows a transient inline message on a premature click (&quot;Select 1 more test to enable Compare.&quot; / &quot;Select 2 completed tests above first.&quot;) via <code>aria-disabled</code> + a polite <code>aria-live</code> region #240.<br>
- <code>deploy/update.sh</code> — a single operator command that pulls the latest images, restarts services with <code>up -d --remove-orphans</code>, prints <code>docker compose ps</code>, and tails logs for 10 seconds so first-boot errors land in the same terminal. <code>--mode {all-in-one|server|testrunner}</code> picks the right production compose file; <code>--version X.Y.Z</code> rewrites <code>SITESPEED_IO_SERVER_VERSION</code> / <code>SITESPEED_IO_TESTRUNNER_VERSION</code> in <code>.env</code> atomically before pulling. <code>PRODUCTION.md</code> is updated to call the helper in every place it used to spell out the compose invocation by hand.<br>
- <code>.env.example</code> is now the single source of truth for every variable any compose file reads (dev or production). New header walks through the three deployment shapes, and the previously-undocumented <code>DOMAIN</code> variable (used by the Caddy single-server compose for automatic HTTPS) is included. <code>PRODUCTION.md</code> drops its inline env-var snippets and points at <code>.env.example</code> so the two can&#39;t drift.<br>
<br>
<strong>Fixed</strong><br>
- <code>SIGTERM</code> / <code>SIGINT</code> now actually drain in-flight HTTP requests before the process exits. The previous shutdown chain ended at <code>this.app.close()</code> on the Express app object (no such method exists), so the HTTP server was never told to stop — it just disappeared when <code>process.exit(0)</code> fired a few lines later, killing in-flight requests mid-response. The web server now keeps the real <code>http.Server</code> reference and calls <code>close()</code> on it; a configurable <code>server.shutdown.timeoutMs</code> (default 30 s) caps the drain and falls back to <code>closeAllConnections()</code> after the deadline. A second signal during shutdown short-circuits to immediate exit for operators who&#39;d rather not wait #243.<br>
- Custom hero / navbar logo overrides now use whatever dimensions the operator sets in <code>html.logo.*</code>. Two hardcoded <code>aspect-ratio: 90.488 / 19.543</code> / <code>aspect-ratio: 123 / 25.375</code> CSS rules were forcing every logo back to the bundled SVG&#39;s proportions #239.<br>
- The analyze button on the Extras tab no longer sits a few pixels lower than on the other tabs. <code>#extras .control</code> was applying <code>padding: 4px 0</code> to the submit button&#39;s wrapper as well as to the intended checkbox rows; scoped to <code>.extras-fieldset .control</code> so only the diagnostics checkboxes get the extra spacing #239.<br>
- Footer spacing: removed the duplicate <code>&amp;nbsp;</code> before &quot;version&quot; and added one between the trailing dash and &quot;AGPLv3&quot;, so the footer now reads &quot;©sitespeed.io  2026 - version X.Y.Z - AGPLv3&quot; with consistent spacing #239.<br>
- Search help panel: tighter copy. &quot;You can combine your search commands. To find a test for a URL, date and browser you can search like this&quot; is now &quot;Combine commands to narrow your search. Example&quot;. The &quot;Find tests by …&quot; preamble that repeated on every line is gone — each command now leads with its field name and (where applicable) the allowed values #239.<br>
<br>
<strong>Security</strong><br>
- Stop logging and echoing back invalid API keys. <code>validateKey</code> used to write the rejected value to the server log and return it in the 403 response body, so typos and stale secrets piled up in log aggregation. The log line is now <code>Invalid API key supplied</code> (no value, no length) and the response carries a single &quot;Invalid API key.&quot; message in every locale #242.<br>
- Close two XSS vectors that the recent UI work uncovered:<br>
- Enforce a defense-in-depth CSP (<code>default-src &#39;self&#39;</code>; <code>script-src</code> / <code>style-src</code> allow <code>&#39;unsafe-inline&#39;</code> transitionally because pug templates still ship inline blocks; <code>frame-ancestors &#39;none&#39;</code>, <code>object-src &#39;none&#39;</code>, <code>base-uri &#39;self&#39;</code>, <code>form-action &#39;self&#39;</code>) and re-enable HSTS. Both were disabled previously #242.<br>
- Block sensitive sitespeed.io flags from the GUI&#39;s &quot;Command line args&quot; tab. The user-supplied extras textarea used to forward arbitrary flags to the testrunner with only <code>--verbose</code> filtered. A GUI submission could load plugin / JS code (<code>--plugins.add</code>, <code>--browsertime.preScript</code>), run any binary as the browser (<code>--browsertime.chrome.binaryPath</code>), redirect HARs and metrics to an attacker-controlled destination (whole <code>--graphite.*</code> / <code>--s3.*</code> / <code>--gcs.*</code> / <code>--scp.*</code> / <code>--slack.*</code> / <code>--matrix.*</code> / <code>--datadog.*</code> / <code>--influxdb.*</code> namespaces), or read arbitrary files via <code>--config</code>. A denylist now drops these tokens (and their value arguments) before they reach sitespeed.io, and <code>/sitespeed-help.json</code> filters the same set so the CLI picker stops suggesting them (277 → 187 entries). API submissions (which pass a <code>userConfig</code> object, not extras) are deliberately untouched #241.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 40.4.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.4.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.4.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Browsertime 27.2.0 — <code>recalculateStyle</code> is now summarised at the HAR page level as <code>_renderBlocking.recalculateStyle</code> (beforeFCP / beforeLCP element counts + durations), so the metric is reachable from the page entry instead of only inside <code>browsertime.json</code> #4723.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.2.0]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.2.0</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Surface the page-level <code>recalculateStyle</code> summary (beforeFCP / beforeLCP element counts + durations) on the first HAR page as <code>_renderBlocking.recalculateStyle</code>. The per-request <code>_renderBlocking</code> map already projected onto each entry stays where it is — the page-level summary is what powers the &quot;Elements that needed recalculate style before FCP&quot; view and was previously only reachable via <code>browsertime.json</code> #2466.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.5.0]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.5.0</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.5.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Surface page-level style recalculation work on <code>renderBlocking.recalculateStyle</code><br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 2.0.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#2.0.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#2.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Swapped the waterfall renderer from PerfCascade to<br>
- Page X-ray table grouped into sections (Content, Render blocking,<br>
- Filmstrip section (when both HARs have one) with frames sampled from<br>
- Visual progress chart now shows per-HAR timing markers<br>
- Core Web Vitals row in the page X-ray table:<br>
- Render-blocking metrics from Chrome HARs (blocking, potentially<br>
- Long Tasks, Total Blocking Time and Max Potential FID under CPU.<br>
- <code>stripVersion</code> configuration flag (and a checkbox on the start page)<br>
- Support for <code>.har.gz</code> HAR files via the browser&#39;s native<br>
- Skip-to-content link, ARIA labels and visually-hidden table captions<br>
- Comment slot for the request/response diff section<br>
- First/third party support — PageXray now auto-detects the host page&#39;s<br>
<br>
<strong>Changed</strong><br>
- Build tooling moved to Vite. The build output<br>
- Styling rewritten in plain CSS (Sass dropped) with a small design-token<br>
- Templates rewritten as plain JS template literals (Template7 dropped).<br>
- Upgraded PageXray to 4.x.<br>
<br>
<strong>Removed</strong><br>
- The service worker / Workbox offline support. A one-time cleanup<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.3.1]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.3.1</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.3.1"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Filmstrip 404s in the compare view, again — the 3.3.0 pass still constructed frame URLs on a fixed 100 ms cadence inside <code>(FirstVisualChange, LastVisualChange)</code>, which broke whenever visual progress sat flat between two boundaries (the expected JPGs were never written to disk). The vendored compare bundle now reads frame timestamps straight from <code>_visualMetrics.VisualProgress</code> — one JPG per VP sample, named after that exact ms — and lets the existing 100 ms padding step forward-fill the uniform grid, so the rendered strip still reads &quot;nothing happened for 2 s&quot; differently from &quot;everything changed in 50 ms&quot; #238.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.3.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.3.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.3.0"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Refreshed the vendored compare bundle with a sharing pass, accurate transfer-size diffs and style recalculation visibility:<br>
<br>
<strong>Fixed</strong><br>
- Filmstrip and Visual Progress thumbnails no longer 404 — frame timestamps are now derived from the same <code>_visualMetrics.VisualProgress</code> change points sitespeed.io&#39;s own report uses, matching the cadence sitespeed.io actually wrote to disk.<br>
- Millisecond timings in the page-x-ray diff are rounded to whole ms — the new recalc duration field reports sub-ms precision (&quot;17.099 ms&quot;) that&#39;s noise at this scale.<br>
- &quot;Switch HAR&quot; and run-dropdown changes preserve the previous source URLs / config / stripVersion choice, so the Share UI doesn&#39;t downgrade from &quot;Copy share link&quot; to &quot;Download bundle&quot; on every toggle.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[onlinetest 3.2.0]]></title>
        <id>https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.2.0</id>
        <link href="https://github.com/sitespeedio/onlinetest/blob/main/CHANGELOG.md#3.2.0"/>
        <updated>2026-05-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Refreshed the vendored compare bundle to pick up the upstream regression-spotting pass:<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 40.3.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.3.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.3.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-10T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Bump the bundled Docker browsers to Chrome 148, Firefox 150, Edge 147 #4709.<br>
<br>
<strong>Fixed</strong><br>
- Compare plugin no longer crashes when running with <code>--verbose</code> — a typo in a <code>log.verbose</code> call (<code>&#39;%j&#39;.results</code> instead of <code>&#39;%j&#39;, results</code>) threw once the Python statistical tests returned.<br>
- <code>coach-core</code> bumped to 9.0.1 #4703.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 9.0.1]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.0.1</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.0.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2026-05-07T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- The <code>mimeTypes</code> HAR performance check no longer flags <code>204 No Content</code> responses as missing a recognised content type. A 204 response has no body, so there is nothing to attribute a MIME type to and the rule should not subtract score for it. Thank you Nate Spector for PR #161.<br>
- The <code>nelHeader</code> and <code>reportingEndpointsHeader</code> HAR privacy rules are now weight 0 so they no longer pull on the aggregate score. Both rules are tagged <code>severity: &#39;info&#39;</code> and describe genuinely opt-in observability headers — most sites have no Reporting-API endpoint to point them at and should not be docked privacy score for that. The advice still surfaces as informational, matching the established pattern in <code>mimeTypes</code> #162.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[sitespeed.io 40.2.0]]></title>
        <id>https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.2.0</id>
        <link href="https://github.com/sitespeedio/sitespeed.io/blob/main/CHANGELOG.md#40.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/sitespeed.io.png" type="image/png"/>
        <updated>2026-05-06T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Update the CPU tab for Chrome to show more valuable metrics #4691.<br>
- Add a scrubbable timeline to the visual-progress card #4690.<br>
- Style the Server timings table to match the rest of the metrics tab #4689.<br>
- Make Domains, Toplist and Assets pages easier to scan/read #4688<br>
- A friendlier third-party tab #4687<br>
<br>
<strong>Fixed</strong><br>
- Update to Browsertime 27.1.0 #4691<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.1.0]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.1.0</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.1.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-06T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Three new analyses on <code>result.cpu</code> derived from the same Chrome <code>trace.json</code> that&#39;s already collected when <code>--cpu</code> is on. <code>scriptCosts</code> produces the per-URL parse / compile / execute / total breakdown that Lighthouse&#39;s bootup-time audit shows, sorted by total descending. <code>forcedReflows</code> walks the main-thread task tree for <code>Layout</code> / <code>UpdateLayoutTree</code> events nested inside JS-driven tasks (<code>EventDispatch</code>, <code>FunctionCall</code>, <code>TimerFire</code>, <code>FireAnimationFrame</code>, …) — every match is a synchronous reflow caused by JavaScript reading a layout-triggering property mid-handler, reported with the script that triggered it. <code>nonCompositedAnimations</code> surfaces <code>Animation</code> events whose <code>compositeFailed</code> bitmask is non-zero, returning the unsupported properties (<code>top</code>, <code>box-shadow</code>, <code>filter</code>, …) so consumers can see what to swap for the GPU-friendly equivalent. Each new analysis is wrapped in its own try / catch so a bug in one can&#39;t poison the existing <code>categories</code> / <code>events</code> / <code>urls</code> payload #PR1.<br>
<br>
<strong>Fixed</strong><br>
- Replace the unmaintained <code>@sitespeed.io/tracium</code> dependency (extracted from Lighthouse circa 2017, last release 0.3.3) with an in-tree ESM port at <code>lib/chrome/trace/</code>. The algorithm is a 1:1 port; output of <code>computeMainThreadTasks</code> is byte-equivalent for any trace, so existing <code>result.cpu.{categories,events,urls}</code> consumers see no change. Pulling the parser inline lets new analyses ship at Browsertime&#39;s release cadence without a second <code>npm publish</code> step in between #PR1.<br>
- Modernise the trace-event classifier. The original list only knew about ~30 event names, so on busy 2026-era pages roughly half the trace fell through to &quot;other&quot; — on a sample cnet run that meant 1.3 s of &quot;other&quot; hiding <code>RunTask</code>, <code>v8.run</code>, <code>IntersectionObserverController::computeIntersections</code>, <code>PrePaint</code>, <code>Commit</code>, <code>Layerize</code>, <code>v8.callFunction</code>, <code>V8.DeserializeContext</code> and friends. The expanded list draws from modern Lighthouse + the WebPageTest-derived map in waterfall-tools + direct sampling of real traces, and a new <code>groupForEvent()</code> lookup adds <code>V8.GC*</code> prefix matching so future V8 GC phases auto-classify #PR1.<br>
- Bump <code>chrome-har</code> to 1.3.0 to pick up the new <code>_renderBlocking</code> field on each entry, lifting Chrome&#39;s CDP <code>renderBlockingStatus</code> (Chrome 108+) so downstream HAR consumers can see which resources blocked first paint without inferring it from the trace.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.3.0]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.3.0</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.3.0"/>
        <updated>2026-05-06T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Lift CDP <code>renderBlockingStatus</code> onto entries as <code>_renderBlocking</code> (Chrome 108+) so downstream HAR consumers (waterfall-tools&#39;<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.0.4]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.4</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.4"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Fix Safari iOS video time scale #2457. This make the video and Safari metric match.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 9.0.0]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.0.0</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#9.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2026-05-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- New <code>severity</code> field on every rule result, with three tiers (<code>error</code> / <code>warn</code> / <code>info</code>) modelled on Lighthouse and axe. Severity is orthogonal to weight: weight controls how strongly a rule pulls on the aggregate score, severity tells a consumer how loudly to surface a finding. Existing <code>score</code> and <code>weight</code> are unchanged #143.<br>
- New <code>interactionToNextPaint</code> DOM performance rule. INP replaced First Input Delay as a Core Web Vital in March 2026, but coach-core had rules for every other Web Vital (LCP, FCP, CLS) and was silently behind on the one Google now ranks against. The rule observes buffered <code>event</code> entries via PerformanceObserver, groups them by <code>interactionId</code> so a single user interaction&#39;s three events (pointerdown, pointerup, click) count as one, and reports the slowest. Thresholds match Google&#39;s published p75 cutoffs: ≤200ms good, 200–500ms needs improvement, &gt;500ms poor. Synthetic tests rarely fire interactions, so the rule explicitly handles the no-measurement case rather than scoring zero — INP is best measured with real-user monitoring and the advice text says so #155.<br>
- Four new DOM performance rules covering modern image best practices: <code>decodingAsync</code> flags <code>&lt;img&gt;</code> elements without a <code>decoding</code> hint so non-LCP image decode doesn&#39;t block the main thread; <code>lazyLoadingImages</code> flags below-the-fold images that aren&#39;t <code>loading=&quot;lazy&quot;</code> (the threshold deliberately overshoots — more than two viewport heights below the current scroll position — so images just out of view aren&#39;t penalised); <code>modernImageFormats</code> flags <code>&lt;img&gt;</code> that ships a JPEG/PNG/GIF without an AVIF or WebP alternative through <code>&lt;picture&gt;</code> or its own <code>srcset</code>; <code>lcpImageHints</code> scores priority hints on the LCP image (<code>fetchpriority=&quot;high&quot;</code> recommended, <code>loading=&quot;lazy&quot;</code> forbidden) #157.<br>
- Modern privacy / security header rules on the HAR side: <code>permissionsPolicyHeader</code>, <code>xContentTypeOptionsHeader</code> #138, and the cross-origin isolation trio <code>crossOriginOpenerPolicyHeader</code>, <code>crossOriginEmbedderPolicyHeader</code>, <code>crossOriginResourcePolicyHeader</code> #139.<br>
- Two DOM privacy rules that the HAR side cannot see: <code>iframeSandbox</code> (flags cross-origin iframes without a sandbox attribute) and <code>referrerPolicy</code> (flags pages with no <code>&lt;meta name=&quot;referrer&quot;&gt;</code>) #139.<br>
- New <code>sessionReplay</code> DOM privacy rule that flags pages embedding session-recording tools (Hotjar, FullStory, Microsoft Clarity, LogRocket, Smartlook, Mouseflow, Quantum Metric, Inspectlet, Lucky Orange, Crazy Egg). Kept separate from the surveillance rule because the remediation is different — these tools have legitimate uses but the privacy concern is real, so the advice is &quot;configure input redaction, get explicit consent, know where the recordings live&quot; rather than &quot;stop using them&quot; #153.<br>
- New <code>nelHeader</code> and <code>reportingEndpointsHeader</code> HAR privacy rules — thin presence checks for the Network Error Logging and Reporting-API headers. Severity <code>info</code> because these are observability headers, not security baselines #150.<br>
- Two DOM bestpractice rules: <code>viewport</code> (flags missing viewport meta, missing <code>width=device-width</code>, and the patterns that disable pinch-to-zoom) and <code>imageAltText</code> (flags <code>&lt;img&gt;</code> without an <code>alt</code> attribute, accepting <code>alt=&quot;&quot;</code>, <code>role=&quot;presentation&quot; / &quot;none&quot;</code> and <code>aria-hidden=&quot;true&quot;</code> as valid markers for decorative images) #140.<br>
- <code>lib/technologies/VERSION.json</code> records source URL, upstream SHA and sync date for the bundled fingerprint catalogue, exposed via a new <code>coach.getTechnologiesVersion()</code> accessor next to <code>getWappalyzerCoreVersion()</code> and <code>getThirdPartyWebVersion()</code> #144.<br>
<br>
<strong>Fixed</strong><br>
- HSTS rule no longer scores 100 silently on an HTTPS page that is missing the Strict-Transport-Security header. Several other rules had small typos and dead branches that masked their advice text or scoring: dangling template literals in the FCP / LCP &quot;good&quot; branches (advice was being built and discarded), an off-by-one in <code>fullyLoaded.js</code> that skipped the first resource, an operator-precedence bug in <code>unnecessaryHeaders.js</code> that silently broke the cache-control + expires check, an assignment instead of <code>-=</code> in <code>thirdPartyCookies.js</code> that flattened the score regardless of cookie count, plus dead <code>score;</code> and <code>if (score === undefined)</code> blocks in HSTS #137.<br>
- Wappalyzer no longer attributes technologies named in a page&#39;s Content-Security-Policy header to the page itself. Wappalyzer regex-matches patterns like <code>s3[^ ]*amazonaws.com</code> against header values, so a benign CSP allowlist (Wikipedia&#39;s CSP names an S3 bucket to permit embedded photos) was getting attributed as &quot;uses S3&quot;. The technology detector now strips <code>content-security-policy*</code> headers before handing the asset response to Wappalyzer, and pins the URL and HTML body to the document itself rather than letting them drift to a later asset in the loop #149.<br>
- <code>lib/har/thirdParty.js</code> no longer mutates the imported <code>third-party-web</code> entity object&#39;s <code>categories</code> array — side-effects could leak across pages and across consumers #137.<br>
- LCP &quot;poor&quot; advice text said &quot;slower than 4.5 seconds&quot; but the threshold is 4 seconds; text now matches the threshold #137.<br>
- Restore the DOM-rule test infrastructure that had been silently dead since the browsertime 23 upgrade. The old runner called <code>loadAndWait</code> and <code>runScript</code> on a <code>BrowsertimeEngine</code> instance; those methods only exist on the internal <code>SeleniumRunner</code> class and were never on the public engine, so the tests blew up the moment they tried to register. The runner is now a thin wrapper around <code>selenium-webdriver</code> (drivers managed by Selenium Manager — no path config required), and the broken async-<code>describe</code> pattern in every DOM test file is replaced with the standard async <code>before</code> / <code>after</code> hooks. <code>npm test</code> continues to run the no-browser suites; <code>npm run test:browser</code> now actually drives a browser instead of silently no-op-ing #148.<br>
<br>
<strong>Changed</strong><br>
- Score the Content-Security-Policy header on its directives instead of its byte length. The previous heuristic (deduct 50 if the header value was longer than 150 chars) didn&#39;t reflect any real-world property and the comparison was reading <code>.length</code> on the array of header values rather than on the value string, so the deduction never actually fired. The new rule deducts for <code>&#39;unsafe-inline&#39;</code> and <code>&#39;unsafe-eval&#39;</code>, for missing <code>default-src</code> / <code>script-src</code>, and for missing <code>object-src &#39;none&#39;</code>; the <code>&#39;unsafe-inline&#39;</code> deduction is suppressed when <code>&#39;strict-dynamic&#39;</code> is paired with a nonce or hash, which is the modern strict-CSP shape #151.<br>
- The <code>largestContentfulPaint</code> rule no longer scores <code>fetchpriority</code> and <code>loading</code> hints on the LCP image. Those checks moved into the new <code>lcpImageHints</code> rule above so a consumer can tell &quot;the hero image is slow&quot; apart from &quot;the hero image&#39;s hints are wrong&quot; — different remediations, separate scores #157.<br>
- Modernise the rule set: delete <code>dom/bestpractice/spdy.js</code> (SPDY no longer in browsers since 2016); rewrite <code>dom/privacy/surveillance.js</code> to scan <code>&lt;script src&gt;</code> and <code>&lt;iframe src&gt;</code> for known surveillance hostnames using exact-suffix host matching (no more <code>document.domain</code>, no more substring matches); update <code>dom/privacy/ga.js</code> to also detect GA4 (<code>gtag</code> + <code>dataLayer</code> config with <code>G-…</code> measurement id); replace the &quot;HTTP/3 is new&quot; placeholder in <code>dom/performance/inlineCss.js</code> with real advice; reweight <code>amp</code> (10 → 1) and <code>jquery</code> (4 → 1); drop SPDY from <code>isHTTP2</code> #138.<br>
- Refresh stale rule thresholds across the rule set: <code>metaDescription</code> 155 → 160 chars (matches Google&#39;s modern search-results display); <code>cacheHeadersLong</code> floor 30 days → 1 year (modern best practice for content-hashed URLs with <code>immutable</code>); <code>strictTransportSecurityHeader</code> max-age floor 6 months → 1 year (HSTS preload list minimum); <code>cacheHeaders</code> weight 30 → 6 (in line with the rest of the rule set, which sits at 1–10); <code>pageSize</code> 1 / 2 MB → 2 / 3 MB mobile / desktop (HTTP Archive 2024 medians); <code>cssSize</code> 120 / 400 KB → 150 / 500 KB; <code>javascriptSize</code> 120 / 500 KB → 250 / 800 KB; <code>optimalCssSize</code> 14.5 → 25 KB with the dated &quot;magic number TCP window&quot; framing dropped. Several rule descriptions and advice strings were rewritten to drop dated framings and to point at the modern reference #154.<br>
- Extend the surveillance hostname list from 10 entries to 32, grouped and commented by company (X / Twitter, LinkedIn, TikTok, Snapchat, Pinterest, Reddit, Bing Ads, Yandex Metrica, Baidu Tongji on top of the existing Google / Meta / YouTube set). Session-replay tools deliberately stay out of this list and have their own rule #152.<br>
- Tune severity on four rules that needed editorial intent rather than the weight-derived default: <code>thirdPartyPrivacy</code> (sharing user data with third parties) <code>info</code> → <code>warn</code>; <code>thirdParty</code> (too many third-party requests) <code>info</code> → <code>warn</code>; <code>spof</code> (frontend single point of failure) <code>info</code> → <code>error</code>; <code>googleTagManager</code> <code>info</code> → <code>warn</code> #158.<br>
- Declare an explicit <code>severity</code> on the seven 9.0 rules that still leaned on the <code>severity.fromWeight()</code> fallback, so each value reflects editorial intent: <code>viewport</code> <code>warn</code>; <code>imageAltText</code> <code>info</code>; <code>iframeSandbox</code> <code>info</code>; <code>referrerPolicy</code> <code>warn</code>; <code>crossOriginOpenerPolicyHeader</code> <code>warn</code>; <code>crossOriginEmbedderPolicyHeader</code> <code>info</code>; <code>crossOriginResourcePolicyHeader</code> <code>info</code> #159.<br>
- Sync the bundled technology catalogue (<code>lib/technologies/*.json</code>, <code>categories.json</code>, <code>groups.json</code>) from <code>enthec/webappanalyzer@c2855b46</code> (2026-04-17). About sixteen months of upstream churn since the previous sync, net ~36 000 lines of new fingerprints #144.<br>
- Bump <code>third-party-web</code> to 0.29.0 #145.<br>
- Drop <code>bluebird</code> from runtime dependencies — native promises cover everything coach-core needs and the package was effectively unused #156.<br>
- Modernise the GitHub Actions workflow: bump <code>actions/checkout</code> to v6.0.2, <code>actions/setup-node</code> to v6.4.0 (with npm cache enabled), replace the deprecated apt-key Chrome install with a one-liner, replace the unmaintained third-party <code>xvfb-action</code> with <code>xvfb-run</code>, expand the Node matrix to 20 / 22 / 24, drop test/dom and test/api/domTest.js from <code>npm test</code> (test runner needs a separate rewrite — see #147 if you want them back). New <code>dependabot.yml</code> keeps SHA pins current going forward #141.<br>
- Bump browsertime devDep to 27.0.0 (no runtime impact — coach-core has no runtime browsertime dep, only the dev test runner used to).<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.0.3]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.3</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.3"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-04T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Fix so INP only is calculated on interactions with an id #2456.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.0.2]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.2</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.2"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- SPA scripts no longer drop a HAR page when Chrome&#39;s soft-navigation <code>PerformanceObserver</code> entry doesn&#39;t fire. #2438 gated HAR-page creation on Chrome detecting all three soft-navigation criteria (interaction + URL change + visible paint within a short window), but real SPAs like Grafana sometimes paint too late and Chrome never emits the entry — chrome-har then sees only <code>Network.*</code> events for that measurement and emits zero pages, breaking downstream per-iteration page indexing in sitespeed.io. When the measurement was started without a URL (<code>commands.measure.start(alias)</code> form) and no soft-nav entry was detected, fall back to a synthetic page anchored at the current <code>location.href</code> so each <code>measure.start</code> produces exactly one HAR page.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.0.1]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.1</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- <code>commands.click</code> now falls back to a JavaScript click when the Actions API can&#39;t reach the element. Hidden elements have a zero-size bounding box, so the Actions API click that became the default in #2381 silently moved the pointer to (0,0) and clicked nothing instead of triggering the handler. The click command now pre-checks <code>isDisplayed()</code> and uses a JS click when the element isn&#39;t displayed, and otherwise catches any Actions API failure and retries with a JS click. Each fallback is logged at info level so it&#39;s visible without <code>--verbose</code>. Restores the documented &quot;hide everything before clicking&quot; pattern used by visual-metric scripts #2452.<br>
- <code>engineDelegate.afterBrowserStopped()</code> is no longer called twice per iteration on the happy path. #2432 added a safety-net cleanup call in the iteration <code>finally</code> block so failure paths still release long-lived helpers, but in the happy path <code>_stopBrowser</code> had already invoked the cleanup, so it ran twice. The visible symptom on Chrome was a spurious <code>ENOENT</code> error from the second <code>cleanUserDataDir</code> (the first call had already removed the directory). It could also double-terminate the iOS simulator&#39;s Safari and the <code>ios-capture</code> server. The safety-net now runs only when <code>_stopBrowser</code> didn&#39;t already reach the cleanup #2453.<br>
- <code>cleanUserDataDir</code> cleanup now uses <code>rm(..., { recursive: true, force: true })</code> so a missing directory is treated as already-cleaned. Defense in depth alongside #2453: if anything in the future double-calls the Chrome cleanup, or if Chrome failed to create the directory in the first place, the cleanup stays quiet instead of logging a spurious <code>ENOENT</code> #2454.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[browsertime 27.0.0]]></title>
        <id>https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.0</id>
        <link href="https://github.com/sitespeedio/browsertime/blob/main/CHANGELOG.md#27.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/browsertime.png" type="image/png"/>
        <updated>2026-05-01T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Unified selector syntax for all interaction commands. You can now call <code>commands.click(&#39;#btn&#39;)</code>, <code>commands.addText(&#39;#input&#39;, &#39;text&#39;)</code>, <code>commands.wait(&#39;#el&#39;, { timeout: 5000 })</code>, <code>commands.select(&#39;#dropdown&#39;, &#39;value&#39;)</code>, and <code>commands.set(&#39;#field&#39;, &#39;value&#39;)</code> directly. Supports prefixes: <code>id:</code>, <code>xpath:</code>, <code>text:</code>, <code>link:</code>, <code>name:</code>, <code>class:</code> — CSS selector is the default #2390 #2408.<br>
- The <code>AndWait</code> methods are deprecated in favour of <code>{ waitForNavigation: true }</code> #2390 #2396.<br>
- Navigation methods (back, forward, refresh) now support <code>{ waitForNavigation: true }</code> for consistency #2408.<br>
- Added <code>commands.type(selector, text)</code> as a shorthand for <code>addText</code> with a more conventional parameter order #2387.<br>
- Added <code>commands.find(selector, options)</code> helper that combines element lookup with optional waiting and visibility checks #2386.<br>
- Added <code>commands.measure.clickAndMeasure(alias, selector)</code> shortcut for the common measure.start/click/measure.stop pattern #2382.<br>
- Added <code>commands.click.byText(text)</code> to click any element by visible text, not just links #2384.<br>
- Added <code>--timeouts.elementWait</code> option for auto-waiting on elements before interaction #2383.<br>
- Added support for TypeScript scripts (.ts, .mts, .cts) using Node.js native type stripping (requires Node.js 22.18.0+) #2363.<br>
- Added <code>commands.getText(selector)</code>, <code>commands.getValue(selector)</code>, and <code>commands.isVisible(selector)</code> convenience methods #2411.<br>
- Added <code>commands.exists(selector, { timeout })</code> for checking element presence without throwing #2412.<br>
- Added <code>commands.clear(selector)</code> to clear form element content #2413.<br>
- Added <code>commands.fill({ selector: text, ... })</code> for filling multiple form fields at once #2414.<br>
- Added unified selector syntax to mouse commands: <code>commands.mouse.singleClick(selector)</code>, <code>commands.mouse.doubleClick(selector)</code>, <code>commands.mouse.contextClick(selector)</code>, <code>commands.mouse.moveTo(selector)</code> #2415.<br>
- Added <code>commands.cookie</code> for managing browser cookies: <code>get</code>, <code>getAll</code>, <code>set</code>, <code>delete</code>, <code>deleteAll</code> #2420.<br>
- Added <code>commands.getAttribute(selector, name)</code>, <code>commands.isEnabled(selector)</code>, and <code>commands.isChecked(selector)</code> for element state queries #2421.<br>
- Added <code>commands.hover(selector)</code> as alias for mouse.moveTo, <code>commands.press(key)</code> for keyboard input, <code>commands.getTitle()</code> and <code>commands.getUrl()</code> for page info #2422.<br>
- Added <code>commands.check(selector)</code> and <code>commands.uncheck(selector)</code> for checkbox/radio interactions #2423.<br>
- Added <code>commands.scrollIntoView(selector)</code> to scroll elements into the viewport #2425.<br>
- Added <code>commands.select.byText(selector, text)</code> to select dropdown options by visible text #2426.<br>
- Added <code>commands.waitForUrl(pattern, { timeout })</code> to wait for URL changes after navigation #2427.<br>
- Added video recording and visual metrics for Safari on iOS over USB. A native helper captures the device&#39;s screen via CoreMediaIO and pipes frames to ffmpeg. The device is woken into screen-capture mode automatically — no manual QuickTime step required #2432 #2436.<br>
- Added HAR capture for Safari on iOS via <code>ios_webkit_debug_proxy</code>. Browsertime starts and stops <code>iwdp</code> automatically and exits with a clear error if it isn&#39;t installed (<code>brew install ios-webkit-debug-proxy</code>). New <code>--safari.includeResponseBodies</code> CLI option. macOS Safari is unaffected #2431.<br>
- Added soft navigation support for Chrome. Single-page app route changes (React, Next.js, Vue, Turbo) now produce a HAR page and the standard FCP/LCP/CLS/INP metrics, using Chrome&#39;s PerformanceObserver soft-navigation entry type to confirm user interaction + URL change + visible paint #2438.<br>
- Updated dependencies to latest compatible versions: chrome-har 1.2.1, selenium-webdriver 4.43.0, execa 9.6.1, jimp 1.6.1 #2437.<br>
- Firefox 149 in the Docker container #2375.<br>
- Record the full Chrome/Edge configuration browsertime applied to the browser (args, preferences, mobile emulation, binary path, extensions count) under <code>info.browser</code> in the result JSON, not just what the user passed via <code>--chrome.args</code> #2447.<br>
- Record Firefox preferences and the resolved binary path under <code>info.browser</code> in the result JSON #2448.<br>
- Write long-task ranges and user-timing marks/measures into the HAR under <code>pages[].pageTimings</code> as <code>_longTasks</code> (array of <code>[startMs, endMs]</code> tuples) and <code>_userTimings</code> (object keyed by entry name → startTime), so downstream waterfall viewers can show what the browser was doing between requests #2449.<br>
- Added modern device profiles to the mobile emulation device list #2446.<br>
<br>
<strong>Fixed</strong><br>
- Pin the iOS Safari tab being measured so unrelated open tabs cannot clobber the active inspector target. The WebDriver tab is navigated to a sentinel URL and identified by it before iWDP messages are processed, otherwise an unrelated <code>Target.targetCreated</code> event could leave the HAR empty #2440.<br>
- Fixed <code>drawtext</code> failing on ffmpeg 8 by quoting font paths and forwarding ffmpeg stderr so font/filter errors are visible #2435.<br>
- Fixed Firefox network idle timeout on repeated iterations. The WebSocket message listener was never removed between iterations, causing the inflight counter to go negative #2391.<br>
- Fixed Firefox network idle timeout when no network events arrive. Timestamps were initialized as undefined causing the idle check to never trigger #2394.<br>
- Fixed intermittent crash when setting orange background before document.body exists on Edge/Windows #2393.<br>
- Element interaction error messages now include the current page URL for easier debugging #2385.<br>
- Added <code>actions.clear()</code> after <code>perform()</code> in all mouse commands to prevent pointer/key state leaking between actions #2406.<br>
- Standardized error handling in all command <code>run()</code> methods using <code>executeCommand</code> with URL context. Fixed typo in select.deselectById and removed dead code in wait #2410.<br>
- Added clear error message when loading TypeScript scripts on Node.js older than 22.18.0 #2404.<br>
- Disabled the &quot;You can zoom in and out&quot; popup on Android #2378.<br>
- Warn when <code>--viewPort</code> is set with a width below 500px on desktop Chrome. Chrome enforces a hard ~500px minimum window width and silently clamps narrower requests, but browsertime kept recording the filmstrip and video at the requested size, leaving captures cropped. The warning points users at mobile emulation, which lays out at the requested size while the OS window stays desktop-sized #2445.<br>
- Stop a perceptual SSIM failure from discarding every other visual metric for the iteration. <code>calculate_perceptual_speed_index</code> now returns <code>(None, None)</code> on internal errors instead of letting the exception propagate up through <code>calculate_visual_metrics</code>, so SpeedIndex, FCP, LCP and the rest still flow through when SSIM can&#39;t run #2444.<br>
- Drain ffmpeg/ffprobe stderr after the process exits in <code>visualmetrics-portable.py</code>. The previous Popen+poll loop dropped output buffered between the last read and exit — silently losing <code>keep pts:</code> lines and therefore filmstrip frames — and could deadlock when ffmpeg&#39;s debug output filled the OS pipe buffer #2443.<br>
- Converted <code>visualmetrics-portable.py</code> path handling to <code>pathlib</code> for consistent cross-platform behaviour #2442.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.2.1]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.2.1</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.2.1"/>
        <updated>2026-04-22T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Make sure not multiple pages are created for a soft nav #154<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.2.0]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.2.0</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.2.0"/>
        <updated>2026-04-22T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Support for soft navigation coming from Browsertime #153<br>
<br>
<strong>Fixed</strong><br>
- Get Charset from response #152<br>
- Fix to parse postData #150<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 6.0.0]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#6.0.0</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#6.0.0"/>
        <updated>2026-03-23T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Added IPv6 throttling support on macOS #94 and Linux #100.<br>
- Apply packet loss also to the upload interface #69.<br>
<br>
<strong>Fixed</strong><br>
- More robust detection of the default network interface on Linux, falling back to global IP address lookup when the default route is unavailable #104.<br>
- Fixed README profile values for DSL and Edge to match actual code, and fixed Node.js API examples to use named imports.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.1.1]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.1.1</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.1.1"/>
        <updated>2025-10-17T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- The option to clean header values was broken, this has been reverted for now.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.1.0]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.1.0</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.1.0"/>
        <updated>2025-10-16T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Add option to clean header values #141.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 8.1.3]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.3</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.3"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2025-08-04T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Reverted svg images in avoidScalingImages check released in 8.1.2 since it was causing errors. Lets work on a fir for it later and revert it for now #135.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 8.1.2]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.2</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.2"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2025-08-01T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Don&#39;t check svg images in avoidScalingImages check, thank you Marcus Müller for PR #129.<br>
- Add zstd to encoding check, thank you Marcus Müller for PR #128.<br>
- Make sure we use wappalyzer that is MIT #133.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 8.1.1]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.1</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2024-12-28T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Fix broken minified release.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 8.1.0]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.0</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.1.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2024-12-28T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Use latest data from https://github.com/enthec/webappanalyzer #114.<br>
- Use third part web 0.26.2 #113<br>
<br>
<strong>Fixed</strong><br>
- Update dev dependencies  #115,  #116 and #117.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.0.1]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.0.1</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.0.1"/>
        <updated>2024-10-11T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Try/catch broken dates #130.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 1.0.0]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.0.0</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#1.0.0"/>
        <updated>2024-10-08T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Removed dayjs dependency #128.<br>
- Removed uuid dependency #127.<br>
- Updated to tough cookie 5.0.0 #126.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 5.0.1]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#5.0.1</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#5.0.1"/>
        <updated>2024-08-26T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Smarter ifb0 handling on Linux #93.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 0.13.5]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#0.13.5</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#0.13.5"/>
        <updated>2024-07-12T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Always take care of Network.responseReceivedExtraInfo even if its fired early, thank you ifduyue for PR #120.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[chrome-har 0.13.3]]></title>
        <id>https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#0.13.3</id>
        <link href="https://github.com/sitespeedio/chrome-har/blob/main/CHANGELOG.md#0.13.3"/>
        <updated>2024-07-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Include early hints requests, thank you Ben Schwarz for PR #118.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach-core 8.0.2]]></title>
        <id>https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.0.2</id>
        <link href="https://github.com/sitespeedio/coach-core/blob/main/CHANGELOG.md#8.0.2"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2023-08-31T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Another update to PageXray with a safer HTTP header check.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.4.4]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.4</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.4"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2023-08-31T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Added a safer header check #125.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.4.3]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.3</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.3"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2023-08-31T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- If the meta section from a HAR file from Browsertime was missing, we didn&#39;t catch that. Fixed in #124.<br>
- Update to latest minimist #122.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 5.0.0]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#5.0.0</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#5.0.0"/>
        <updated>2022-07-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Changed</strong><br>
- Dropped support for NodeJS 12 since it&#39;s unmaintaned.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 4.0.3]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.3</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.3"/>
        <updated>2022-06-29T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Fixed <code>--version</code> so it actually shows the version again #75.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 4.0.2]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.2</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.2"/>
        <updated>2022-06-21T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- The E6 Module convertion in 4.0 broke version check (<code>--version</code>) making installs failed. PR #74 is a temporaty fix for that.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 4.0.1]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.1</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.1"/>
        <updated>2022-06-17T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Removed the dependecy of route. Using <code>ip route</code> instead and we already have <code>ip</code> as a requirement.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.4.2]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.2</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.2"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2022-05-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Added missing SSL timings to the time per domain #117<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 3.1.1]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.1.1</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.1.1"/>
        <updated>2022-04-19T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Updated minimist dependency.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 4.0.0]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.0</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#4.0.0"/>
        <updated>2022-04-14T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Updated dev dependencies to latest versions #68<br>
<br>
<strong>Changed</strong><br>
- Converting the project to ES6 module. If you run Throttle by command line it will work exactly as before except that we dropped support for NodeJS 10 #67. You can read Sindre Sorhus ESM package guide.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.4.1]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.1</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2022-04-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Bumped minimist to 1.2.6<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.4.0]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.0</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.4.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2022-02-04T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Fallback if mime types aren&#39;t mapped and use file endings. At the moment we fallback for some of the font types #109.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 3.1.0]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.1.0</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.1.0"/>
        <updated>2021-12-26T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Add support for setting packet loss. Add <code>--packetLoss</code> and set the loss in percentage.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 7.0.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#7.0.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#7.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2021-12-01T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Updated to use latest Coach core and Browertime.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 6.4.3]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#6.4.3</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#6.4.3"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2021-10-15T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Updated to use latest Browsertime with latest Chromedriver<br>
- Updated Docker container to use latest Chrome/Firefox<br>
- Use Browsertime 11.0.0<br>
- Use Coach-code 6.0<br>
- Use latest Firefox and Chrome<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[throttle 3.0.0]]></title>
        <id>https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.0.0</id>
        <link href="https://github.com/sitespeedio/throttle/blob/main/CHANGELOG.md#3.0.0"/>
        <updated>2021-08-09T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Changed</strong><br>
- When you run on Mac OS we changed so localhost traffic is not throttled by default. To throttle on localhost use <code>--localhost</code>. This makes sence if you use WebDriver to drive your browser on your local machine. And Mac OS now works the same as Linux.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.3.1]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.3.1</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.3.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2021-07-21T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Added missing in_body_parser_blocking render blocking info for Chrome.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.3.0]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.3.0</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.3.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2021-06-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Include request method per asset #105.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[pagexray 4.2.0]]></title>
        <id>https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.2.0</id>
        <link href="https://github.com/sitespeedio/pagexray/blob/main/CHANGELOG.md#4.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/pagexray.png" type="image/png"/>
        <updated>2021-05-28T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Attach info about render blocking assets coming/working in Chrome 92 #104.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 5.0.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#5.0.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#5.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2020-03-11T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Changed</strong><br>
- We moved out the core functionality to coach-core. This makes it easy to use the Coach in your own code.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.5.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.5.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.5.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2020-02-20T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.4.1]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.4.1</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.4.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2020-02-07T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Upgraded to Browsertime 8.0.1<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.4.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.4.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.4.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2020-02-05T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[humble 0.2.0]]></title>
        <id>https://github.com/sitespeedio/humble/blob/main/CHANGELOG.md#0.2.0</id>
        <link href="https://github.com/sitespeedio/humble/blob/main/CHANGELOG.md#0.2.0"/>
        <updated>2020-01-11T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Updated throttle frontend to latest version where you can see throttling profiles set by using the API and with custom throttling support.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.3.1]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.3.1</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.3.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2020-01-10T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.3.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.3.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.3.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2019-12-12T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[coach 4.2.0]]></title>
        <id>https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.2.0</id>
        <link href="https://github.com/sitespeedio/coach/blob/main/CHANGELOG.md#4.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/coach.png" type="image/png"/>
        <updated>2019-11-02T00:00:00.000Z</updated>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 1.0.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#1.0.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#1.0.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-11-28T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Added support for using Github gists see #34.<br>
- Added support for using a config file to configure which HARs to test.<br>
- Added support for copy/paste directly you HAR, configuration file or a gist URL or id.<br>
- Refactor the CSS into Sass, development build process now uses BrowserSync.<br>
- Service worker added for offline support. Workbox is used to achieve this.<br>
- Added support for showing changes in request/responses if you compare the same page #63.<br>
<br>
<strong>Changed</strong><br>
- New internal structure and use the new configuration object all the way.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.6.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.6.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.6.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-10-17T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Sort CPU events by name to make it easier to spot regressions<br>
- Upgraded to latest release of template7<br>
- Added more Visual Metrics.<br>
- Upgrad to PageXray 2.4.0<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.5.1]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.5.1</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.5.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-06-01T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Layout -&gt; Rendering in latest Browsertime.<br>
- Upgraded to PerfCascade 2.5.2 fixiing Edge tab bug<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.5.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.5.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.5.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-04-05T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- CPU metrics are now showed automatically.<br>
- Styled the PageXray info table to make it easier to read.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.4.1]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.4.1</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.4.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-04-04T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Major code refactoring to make it easier to do changes in the future.<br>
- Uploading files failed on Firefox.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.4.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.4.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.4.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-04-02T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Upgraded to PerfCascade 2.4.1<br>
- You can change/upload HAR files when you already compare two HARs.<br>
<br>
<strong>Fixed</strong><br>
- The layout in the PageXray table was dependent of the length of the URL. That could make some pages look really bad.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.3.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.3.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.3.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-03-30T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Automatically load the HAR files if both files are given in the URL . Thanks Ivru for the PR #15.<br>
- You can automatically load one HAR file by adding ?har1=URL&amp;compare=1 as the full URL.<br>
- Updated to PageXray 2.2.1<br>
- You can now drag and drop one HAR file with multiple pages and compare the pages with each others #16.<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.2.1]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.2.1</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.2.1"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-02-03T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Fixed</strong><br>
- Upgraded to PerfCascade 2.2.2 that makes HAR files from WebPageTest Linux render<br>
- Log errors to the console<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
    <entry>
        <title type="html"><![CDATA[compare 0.2.0]]></title>
        <id>https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.2.0</id>
        <link href="https://github.com/sitespeedio/compare/blob/main/CHANGELOG.md#0.2.0"/>
        <link rel="enclosure" href="https://www.sitespeed.io/img/logos/compare.png" type="image/png"/>
        <updated>2018-02-01T00:00:00.000Z</updated>
        <summary type="html"><![CDATA[<strong>Added</strong><br>
- Upgraded to PageXray 2.1.0 and added more CPU metrics<br>
]]></summary>
        <author>
            <name>Sitespeed.io</name>
            <uri>https://www.sitespeed.io</uri>
        </author>
    </entry>
</feed>