- var videoIndex = runIndex ? runIndex : 0;
- var width = options.mobile ? 'max-width: 400px;' : 'width: 100%'

div(style=width)
  video.video-js.vjs-default-skin.vjs-big-play-centered(controls, width='100%', height='auto', preload='none', data-setup='{ "fluid": true, "playbackRates": [0.1, 0.2, 0.5, 1] ,"volumeControl": false }')
    source(src='data/video/' + videoIndex +'.mp4', type='video/mp4')

script(src= rootPath + 'js/video.min.js')
