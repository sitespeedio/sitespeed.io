h2 Page summary
a#pagexray

- const pagexray = pageInfo.data.pagexray.run || pageInfo.data.pagexray.pageSummary[0];

p The page uses #{pagexray.httpVersion}

h3 Requests per response code
table
  tr
    th.text Response code
    th.text Total
  each total, code in pagexray.responseCodes
    tr
      td.text #{code}
      td.text #{total}

- totalContentSize = 0
- totalTransferSize = 0
- totalHeaderSize = 0
- totalRequests = 0
h3 Requests and sizes per content type
.responsive
  table
    thead
      tr
        th.text Content
        th Transfer Size
        th Header Size
        th Content Size
        th Request(s)
    tbody    
    each data, type in pagexray.contentTypes
      - totalContentSize += data.contentSize
      - totalTransferSize += data.transferSize
      - totalHeaderSize += data.headerSize > 0 ? data.headerSize : 0
      - totalRequests += data.requests
      tr
        td.text(data-title='type') #{type}
        td(data-title='transferSize') #{h.size.format(data.transferSize)}
        td(data-title='headerSize') #{data.headerSize > 0 ? h.size.format(data.headerSize) : ''}
        td(data-title='contentSize') #{h.size.format(data.contentSize)}
        td(data-title='requests') #{data.requests}
    tr
      td(data-title='Total')  Total
      td(data-title='transferSize') #{h.size.format(totalTransferSize)}
      td(data-title='headerSize') #{h.size.format(totalHeaderSize)}
      td(data-title='contentSize') #{h.size.format(totalContentSize)}
      td(data-title='requests') #{totalRequests}
h3 Expires and last modified stats
.responsive          
  table
    thead
      tr
        th.text type
        th min
        th median
        th max
    tbody    
      tr
        td.text(data-title='Type')  Expires
        td(data-title='min') #{h.time.duration(pagexray.expireStats.min)}
        td(data-title='median') #{h.time.duration(pagexray.expireStats.median)}
        td(data-title='max') #{h.time.duration(pagexray.expireStats.max)}
      tr
        td.text(data-title='Type') Last modified
        td(data-title='min') #{h.time.duration(pagexray.lastModifiedStats.min)}
        td(data-title='median') #{h.time.duration(pagexray.lastModifiedStats.median)}
        td(data-title='max') #{h.time.duration(pagexray.lastModifiedStats.max)}
